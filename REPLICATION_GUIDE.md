# 🚀 STARPULSE 成功模式：網站複製指南 (Replication Guide)

太棒了！既然這套模式已經驗證成功，未來您要開發任何 AI 網頁應用 (Web App)，只需要複製這套「黃金架構」。

這份指南整理了所有關鍵設定，下次您只需要 **「填入內容」**，不用再重新踩雷。

---

## 1. 核心架構 (Tech Stack)
*   **框架**: React + TypeScript (使用 Vite)
*   **樣式**: Tailwind CSS v4 (最簡潔的設定)
*   **AI**: Gemini API (`gemini-flash-latest` 通用版)
*   **部署**: GitHub Pages (自動化 CI/CD)

---

## 2. 關鍵檔案備份 (Copy These!)

未來開新專案時，這幾個檔案是「絕對核心」，請直接沿用：

### A. AI 服務模組 (`services/geminiService.ts`)
這是我們除錯最久的地方，這個版本已經解決了 404/403/429 所有問題。
*   **特點**: 自動處理錯誤、支援 JSON 模式、使用最穩定的模型別名。
*   **改動**: 未來只需修改 `systemInstruction` (系統提示詞) 來定義 AI 的角色 (例如：把「毒舌講評」改成「溫柔占卜師」)。

### B. 部署腳本 (`.github/workflows/deploy.yml`)
這是讓網站自動上線的關鍵。
*   **特點**: 自動注入 API Key、自動編譯、自動推送到 GitHub Pages。
*   **注意**: 記得在 GitHub Repository 的 **Settings > Secrets** 中設定 `VITE_GEMINI_API_KEY`。

### C. 樣式設定 (`index.css`)
Tailwind v4 只需要一行，千萬別再用舊的設定檔：
```css
@import "tailwindcss";
```

### D. Vite 設定 (`vite.config.ts`)
確保 `base` 路徑設定正確，這樣在 GitHub Pages 上才不會破圖：
```typescript
export default defineConfig({
  plugins: [react()],
  base: "/您的專案名稱/", // <--- 每次開新專案記得改這裡
})
```

---

## 3. 下次開發流程 (SOP)

1.  **建立專案**: `npm create vite@latest my-new-app -- --template react-ts`
2.  **安裝依賴**:
    ```bash
    npm install tailwindcss @tailwindcss/postcss postcss
    npm install @google/generative-ai
    ```
3.  **複製貼上**: 把上述的 A, B, C 檔案複製過去。
4.  **修改內容**: 專注在 `App.tsx` 寫您的新創意。
5.  **推送到 GitHub**:
    *   設定 Secrets (`VITE_GEMINI_API_KEY`)。
    *   Push 程式碼，喝杯咖啡，網站就上線了。

---

**恭喜！您現在已經有一套可量產的 AI 網站生產線了。**
